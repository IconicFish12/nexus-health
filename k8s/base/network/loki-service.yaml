apiVersion: v1
kind: Service
metadata:
  name: loki-svc
  namespace: nexus-infra
  labels:
    app: loki
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3100
      targetPort: 3100
  selector:
    app: loki
