apiVersion: v1
kind: Service
metadata:
  name: grafana-svc
  namespace: nexus-infra
  labels:
    app: grafana-dashboard
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 3000
  selector:
    app: grafana-dashboard
