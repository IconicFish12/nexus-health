apiVersion: v1
kind: Service
metadata:
  name: alertmanager-svc
  namespace: nexus-infra
  labels:
    app: alertmanager
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 9093
      targetPort: 9093
  selector:
    app: alertmanager
