apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: appointment-postgres
  namespace: nexus-service
  labels:
    app: appointment-postgres
    app.kubernetes.io/name: appointment-postgres
    app.kubernetes.io/part-of: nexus-healthcare-system
spec:
  serviceName: appointment-postgres-svc
  replicas: 1
  selector:
    matchLabels:
      app: appointment-postgres
  template:
    metadata:
      labels:
        app: appointment-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:17-alpine
          imagePullPolicy: IfNotPresent
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: appointment-service-secret
                  key: DB_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: appointment-service-secret
                  key: DB_PASSWORD
            - name: POSTGRES_DB
              valueFrom:
                configMapRef:
                  name: appointment-service-config
                  key: DB_NAME
          ports:
            - containerPort: 5432
              name: postgres
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            exec:
              command: ["cmd", "pg_isready", "-U", "postgres"]
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            exec:
              command: ["cmd", "pg_isready", "-U", "postgres"]
            initialDelaySeconds: 15
            periodSeconds: 10
          volumeMounts:
            - name: appointment-psql-data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: appointment-psql-data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 10Gi
---
apiVersion: v1
kind: Service
metadata:
  name: appointment-postgres-svc
  namespace: nexus-service
  labels:
    app: appointment-postgres
    app.kubernetes.io/name: appointment-postgres
    app.kubernetes.io/part-of: nexus-healthcare-system
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
  selector:
    app: appointment-postgres
