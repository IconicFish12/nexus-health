apiVersion: apps/v1
kind: Deployment
metadata:
  name: staff-service
  namespace: nexus-service
  labels:
    app.kubernetes.io/name: staff-service
    app.kubernetes.io/part-of: nexus-healthcare-system
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: staff-service
  template:
    metadata:
      labels:
        app: staff-service
    spec:
      initContainers:
        - name: db-migration
          image: nexus-staff-service:1.0-early
          imagePullPolicy: IfNotPresent
          command: ['bunx', 'prisma', 'migrate', 'deploy']
          envFrom:
            - configMapRef:
                name: staff-service-config
            - secretRef:
                name: staff-service-secret
      containers:
        - name: staff-service
          image: nexus-staff-service:1.0-early
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5008
              name: http
            - containerPort: 500058
              name: grpc
          envFrom:
            - configMapRef:
                name: nexus-common-config
            - configMapRef:
                name: staff-service-config
            - secretRef:
                name: staff-service-secret
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 5008
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 5008
            initialDelaySeconds: 15
            periodSeconds: 15
