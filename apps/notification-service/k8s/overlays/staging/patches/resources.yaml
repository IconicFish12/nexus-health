apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: nexus-service
spec:
  template:
    spec:
      containers:
        - name: notification-service
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
