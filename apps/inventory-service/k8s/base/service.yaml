apiVersion: v1
kind: Service
metadata:
  name: inventory-service-svc
  namespace: nh-inventory-service
  labels:
    app: inventory-service
spec:
  type: ClusterIP
  ports:
    - port: 5005
      targetPort: 5005
      name: http
  selector:
    app: inventory-service
