apiVersion: v1
kind: Service
metadata:
  name: auth-service-svc
  namespace: nh-auth-service
  labels:
    app: auth-service
spec:
  type: ClusterIP
  ports:
    - port: 5002
      targetPort: 5002
      name: http
  selector:
    app: auth-service
