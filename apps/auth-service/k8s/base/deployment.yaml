apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: nexus-service
  labels:
    app.kubernetes.io/name: auth-service
    app.kubernetes.io/part-of: nexus-healthcare-system
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      initContainers:
        - name: db-migration
          image: nexus-auth-service:1.0-early
          imagePullPolicy: IfNotPresent
          command: ['bunx', 'prisma', 'migrate', 'deploy']
          envFrom:
            - configMapRef:
                name: auth-service-config
            - secretRef:
                name: auth-service-secret
      containers:
        - name: auth-service
          image: nexus-auth-service:1.0-early
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5002
              name: http
            - containerPort: 500052
              name: grpc
          envFrom:
            - configMapRef:
                name: nexus-common-config
            - configMapRef:
                name: auth-service-config
            - secretRef:
                name: auth-service-secret
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 5002
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 5002
            initialDelaySeconds: 15
            periodSeconds: 15
