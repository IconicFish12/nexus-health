apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: nexus-service
spec:
  template:
    spec:
      containers:
        - name: auth-service
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
