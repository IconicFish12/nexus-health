apiVersion: v1
kind: Service
metadata:
  name: api-gateway-svc
  namespace: nh-api-gateway
  labels:
    app: api-gateway
spec:
  type: ClusterIP
  ports:
    - port: 5000
      targetPort: 5000
      name: http
  selector:
    app: api-gateway
