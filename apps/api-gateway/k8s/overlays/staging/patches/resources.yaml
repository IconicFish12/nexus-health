apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  namespace: nexus-service
spec:
  template:
    spec:
      containers:
        - name: api-gateway
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
