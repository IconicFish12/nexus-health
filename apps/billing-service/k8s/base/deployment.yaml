apiVersion: apps/v1
kind: Deployment
metadata:
  name: billing-service
  namespace: nexus-service
  labels:
    app.kubernetes.io/name: billing-service
    app.kubernetes.io/part-of: nexus-healthcare-system
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: billing-service
  template:
    metadata:
      labels:
        app: billing-service
    spec:
      initContainers:
        - name: db-migration
          image: nexus-billing-service:1.0-early
          imagePullPolicy: IfNotPresent
          command: ['bunx', 'prisma', 'migrate', 'deploy']
          envFrom:
            - configMapRef:
                name: billing-service-config
            - secretRef:
                name: billing-service-secret
      containers:
        - name: billing-service
          image: nexus-billing-service:1.0-early
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5003
              name: http
            - containerPort: 500053
              name: grpc
          envFrom:
            - configMapRef:
                name: nexus-common-config
            - configMapRef:
                name: billing-service-config
            - secretRef:
                name: billing-service-secret
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 5003
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 5003
            initialDelaySeconds: 15
            periodSeconds: 15
