apiVersion: v1
kind: Service
metadata:
  name: emr-service-svc
  namespace: nh-emr-service
  labels:
    app: emr-service
spec:
  type: ClusterIP
  ports:
    - port: 5004
      targetPort: 5004
      name: http
  selector:
    app: emr-service
