apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: ward-postgres
  namespace: nexus-service
  labels:
    app: ward-postgres
    app.kubernetes.io/name: ward-postgres
    app.kubernetes.io/part-of: nexus-healthcare-system
spec:
  serviceName: ward-postgres-svc
  replicas: 1
  selector:
    matchLabels:
      app: ward-postgres
  template:
    metadata:
      labels:
        app: ward-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:17-alpine
          imagePullPolicy: IfNotPresent
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: ward-service-secret
                  key: DB_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ward-service-secret
                  key: DB_PASSWORD
            - name: POSTGRES_DB
              valueFrom:
                configMapRef:
                  name: ward-service-config
                  key: DB_NAME
          ports:
            - containerPort: 5432
              name: postgres
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            exec:
              command: ["cmd", "pg_isready", "-U", "postgres"]
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            exec:
              command: ["cmd", "pg_isready", "-U", "postgres"]
            initialDelaySeconds: 15
            periodSeconds: 10
          volumeMounts:
            - name: ward-psql-data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: ward-psql-data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 10Gi
---
apiVersion: v1
kind: Service
metadata:
  name: ward-postgres-svc
  namespace: nexus-service
  labels:
    app: ward-postgres
    app.kubernetes.io/name: ward-postgres
    app.kubernetes.io/part-of: nexus-healthcare-system
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
  selector:
    app: ward-postgres
