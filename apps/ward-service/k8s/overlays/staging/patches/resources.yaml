apiVersion: apps/v1
kind: Deployment
metadata:
  name: ward-service
  namespace: nexus-service
spec:
  template:
    spec:
      containers:
        - name: ward-service
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
