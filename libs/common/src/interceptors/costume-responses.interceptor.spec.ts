import { CostumeResponsesInterceptor } from './costume-responses.interceptor';

describe('CostumeResponsesInterceptor', () => {
  it('should be defined', () => {
    expect(new CostumeResponsesInterceptor()).toBeDefined();
  });
});
