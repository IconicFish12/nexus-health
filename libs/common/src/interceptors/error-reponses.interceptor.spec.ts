import { ErrorReponsesInterceptor } from './error-reponses.interceptor';

describe('ErrorReponsesInterceptor', () => {
  it('should be defined', () => {
    expect(new ErrorReponsesInterceptor()).toBeDefined();
  });
});
